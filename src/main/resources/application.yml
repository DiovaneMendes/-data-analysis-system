spring:
  application:
    name: data-analysis-system

server:
  port: 9090

batch:
  job: analysisSyncronizationJob
  step: analysisSyncronizationStep

path:
  recording: ${HOME}/data/out/report.done.dat
  reading: ${HOME}/data/in/