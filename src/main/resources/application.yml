spring:
  application:
    name: data-analysis-system

server:
  port: 9090

batch:
  job: analysisSyncronizationJob
  step: analysisSyncronizationStep

path:
  recording: ${HOMEPATH}/data/out/report.done.dat
  reading: ${HOMEPATH}/data/in/